import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import type { ContentItem } from '@/lib/content';
export function Tag({children}:{children:string}){return <span className="inline-flex border border-teal/20 bg-teal/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-teal">{children}</span>}
export function ContentCard({item,kind='publication'}:{item:ContentItem,kind?:string}){return <article className="group relative flex h-full flex-col overflow-hidden border border-[#dce2e1] bg-white p-6 shadow-[0_8px_25px_rgba(11,32,49,.04)] transition duration-300 hover:-translate-y-1 hover:border-teal/50 hover:shadow-[0_18px_40px_rgba(11,32,49,.10)] dark:border-slate-800 dark:bg-slate-900"><div className="absolute left-0 top-0 h-1 w-12 bg-teal transition-all duration-300 group-hover:w-full"/><div className="mb-5 flex items-center justify-between gap-3"><Tag>{item.topic}</Tag><span className="text-[10px] font-bold uppercase tracking-[.12em] text-slate-400">{item.type}</span></div><h3 className="font-serif text-2xl leading-[1.18]"><Link href={`/${kind}s/${item.slug}`} className="hover:text-teal">{item.title}</Link></h3><p className="mt-4 text-sm leading-6 text-slate-600 dark:text-slate-300">{item.abstract}</p><div className="mt-auto flex items-center justify-between border-t border-[#e4e8e7] pt-5 text-[11px] text-slate-500 dark:border-slate-800"><span>{item.author} · {item.date}</span><Link aria-label={`Read ${item.title}`} href={`/${kind}s/${item.slug}`} className="text-teal"><ArrowUpRight size={17}/></Link></div></article>}
