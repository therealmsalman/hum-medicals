'use client';
import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import type { PublishedContent } from '@/lib/published-content';

export function ApprovedSubmissions({collection}:{collection:'paper'|'article'}){const [items,setItems]=useState<PublishedContent[]>([]);const [topic,setTopic]=useState('All');useEffect(()=>{fetch(`/api/published?collection=${collection}`).then(response=>response.ok?response.json():{items:[]}).then(data=>setItems(data.items||[])).catch(()=>setItems([]));},[collection]);const topics=useMemo(()=>Array.from(new Set(items.map(item=>item.topic))).sort(),[items]);const visible=topic==='All'?items:items.filter(item=>item.topic===topic);if(!items.length)return null;return <section className="mt-16 border-t border-[#dce2e1] pt-12"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Approved author work</p><h2 className="mt-2 text-4xl">Recently published submissions</h2></div><select aria-label="Filter approved author work by topic" value={topic} onChange={event=>setTopic(event.target.value)} className="premium-input w-auto"><option>All</option>{topics.map(item=><option key={item}>{item}</option>)}</select></div><div className="mt-7 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{visible.slice(0,9).map(item=><article key={item.id} className="paper-panel p-6"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-teal">{item.topic}</p><h3 className="mt-4 font-serif text-2xl leading-tight"><Link href={`/published/${item.slug}`} className="hover:text-teal">{item.title}</Link></h3><p className="mt-4 text-sm leading-6 text-slate-600 dark:text-slate-300">{item.abstract}</p><div className="mt-5 flex items-center justify-between border-t pt-4 text-xs text-slate-500"><span>{item.author} · {item.date}</span><Link href={`/published/${item.slug}`} aria-label={`Read ${item.title}`} className="text-teal"><ArrowUpRight size={17}/></Link></div></article>)}</div>{!visible.length&&<p className="mt-6 text-sm text-slate-500">No approved author work has been published in this topic yet.</p>}</section>}
