'use client';
import Link from 'next/link';
import { useState } from 'react';
export function AuthForm({mode}:{mode:'signin'|'signup'}){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const isSignup=mode==='signup';async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage('');const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/auth/${isSignup?'signup':'signin'}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json();setBusy(false);if(!response.ok){setMessage(result.message||'Unable to continue.');return;}window.location.assign('/account');}return <div className="paper-panel mx-auto max-w-md p-7 md:p-9"><p className="eyebrow">Hum Medicals account</p><h1 className="mt-3 text-4xl">{isSignup?'Create your account':'Welcome back'}</h1><p className="mt-3 text-sm leading-6 text-slate-600 dark:text-slate-300">{isSignup?'Create an author account to submit work and manage your publishing journey.':'Sign in to access your author workspace.'}</p><form onSubmit={submit} className="mt-7 grid gap-4">{isSignup&&<input name="name" required minLength={2} className="premium-input" placeholder="Full name" aria-label="Full name"/>}<input name="email" required type="email" className="premium-input" placeholder="Email address" aria-label="Email address"/><input name="password" required minLength={8} type="password" className="premium-input" placeholder="Password (at least 8 characters)" aria-label="Password"/><button disabled={busy} className="premium-button disabled:opacity-50">{busy?'Please wait…':isSignup?'Create account':'Sign in'}</button></form>{message&&<p role="alert" className="mt-4 border-l-4 border-red-500 bg-red-50 p-3 text-sm text-red-800">{message}</p>}<p className="mt-6 text-sm text-slate-600 dark:text-slate-300">{isSignup?'Already have an account?':'New to Hum Medicals?'} <Link className="font-bold text-teal" href={isSignup?'/sign-in':'/sign-up'}>{isSignup?'Sign in':'Create an account'}</Link></p></div>}
