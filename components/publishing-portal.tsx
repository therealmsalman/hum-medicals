'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
type Account={name:string;email:string}; const plans=[{id:'article',name:'Educational Article',price:'$39'},{id:'research',name:'Research Manuscript',price:'$79'},{id:'editorial',name:'Editorial Review',price:'$129'}];
export function PublishingPortal(){const [account,setAccount]=useState<Account|null>(null);const [message,setMessage]=useState('');const [submitting,setSubmitting]=useState(false);useEffect(()=>{fetch('/api/auth/me').then(response=>response.json()).then(data=>setAccount(data.user)).catch(()=>setAccount(null));},[]);async function submitWork(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(!account)return;setSubmitting(true);setMessage('');const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch('/api/publishing/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,author:account})});const result=await response.json();setSubmitting(false);setMessage(result.message||'Your submission has been prepared.');if(result.checkoutUrl)window.location.assign(result.checkoutUrl);}return <div className="mt-12 grid gap-8 lg:grid-cols-[.9fr_1.1fr]"><section className="paper-panel p-7"><p className="eyebrow">Author account</p><h2 className="mt-2 text-3xl">Your workspace</h2>{account?<div className="mt-6 border-l-4 border-teal bg-teal/5 p-5"><p className="font-bold text-ink dark:text-white">{account.name}</p><p className="mt-1 text-sm">{account.email}</p><Link href="/account" className="mt-4 inline-block text-sm font-bold text-teal">Manage account →</Link></div>:<div className="mt-6"><p className="text-sm leading-6">Create a secure author account to submit work and continue to payment.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/sign-up" className="premium-button">Create account</Link><Link href="/sign-in" className="outline-button">Sign in</Link></div></div>}<p className="mt-5 text-xs leading-5 text-slate-500">Accounts use secure password hashing and signed HTTP-only sessions.</p></section><section className="paper-panel p-7"><p className="eyebrow">Submit for publication</p><h2 className="mt-2 text-3xl">Prepare your manuscript</h2><form onSubmit={submitWork} className="mt-6 grid gap-4"><input name="title" required disabled={!account} aria-label="Manuscript title" placeholder="Manuscript title" className="premium-input"/><select name="type" required disabled={!account} aria-label="Submission type" className="premium-input"><option>Research article</option><option>Review article</option><option>Case study</option><option>Educational article</option></select><select name="plan" required disabled={!account} aria-label="Publishing plan" className="premium-input">{plans.map(plan=><option key={plan.id} value={plan.id}>{plan.name} — {plan.price}</option>)}</select><textarea name="abstract" rows={5} required disabled={!account} aria-label="Abstract" placeholder="Structured abstract or article summary" className="premium-input"/><label className="flex gap-2 text-xs leading-5"><input name="consent" type="checkbox" required disabled={!account} className="mt-1"/>I understand submission fees cover editorial processing and do not guarantee acceptance or peer-review outcome.</label><button disabled={!account||submitting} className="premium-button w-fit disabled:opacity-50">{submitting?'Preparing checkout…':'Continue to secure payment'}</button></form>{message&&<p role="status" className="mt-4 border-l-4 border-teal bg-teal/5 p-3 text-sm leading-6">{message}</p>}</section></div>}
