import { NextResponse } from 'next/server'; export async function POST(request:Request){const {email}=await request.json();if(!email)return NextResponse.json({error:'Email is required'},{status:400});/* Connect Mailchimp or ConvertKit here. */return NextResponse.json({ok:true})}
