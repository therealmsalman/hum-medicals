import { NextResponse } from 'next/server';
import { createUser, publicUser, setSession } from '@/lib/auth';
export async function POST(request:Request){try{const {name,email,password}=await request.json();if(typeof name!=='string'||name.trim().length<2)return NextResponse.json({message:'Enter your full name.'},{status:400});if(typeof email!=='string'||!/^\S+@\S+\.\S+$/.test(email))return NextResponse.json({message:'Enter a valid email address.'},{status:400});if(typeof password!=='string'||password.length<8)return NextResponse.json({message:'Use a password with at least 8 characters.'},{status:400});const user=createUser(name,email,password);setSession(user);return NextResponse.json({user:publicUser(user)},{status:201});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:'Unable to create account.'},{status:400});}}
