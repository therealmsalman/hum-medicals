import Link from 'next/link';
import { redirect } from 'next/navigation';
import { currentUser } from '@/lib/auth';
import { AccountActions } from '@/components/account-actions';
export const metadata={title:'My account'};
export default function Account(){const user=currentUser();if(!user)redirect('/sign-in');return <section className="mx-auto max-w-4xl px-5 py-20"><p className="eyebrow">Author workspace</p><h1 className="section-title">Welcome, {user.name}.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600 dark:text-slate-300">Your Hum Medicals author account is active. Start a submission when your manuscript is ready, or return to the journal for new reading.</p><div className="mt-10 grid gap-5 md:grid-cols-2"><Link href="/publish" className="paper-panel p-7 transition hover:-translate-y-1 hover:border-teal"><p className="eyebrow">Submit work</p><h2 className="mt-4 text-3xl">Publish with us</h2><p className="mt-3 text-sm leading-6">Prepare an educational article or research manuscript for editorial consideration.</p></Link><Link href="/publications" className="paper-panel p-7 transition hover:-translate-y-1 hover:border-teal"><p className="eyebrow">Keep learning</p><h2 className="mt-4 text-3xl">Explore the journal</h2><p className="mt-3 text-sm leading-6">Browse evidence-informed clinical publications and articles.</p></Link></div><AccountActions name={user.name}/></section>}
